"use strict";
/**
 * Friends don't let friends use .env
 */
Object.defineProperty(exports, "__esModule", { value: true });
function proclaim(envs) {
    return (key) => {
        const developmentValue = envs[key];
        if (process.env.NODE_ENV !== "production") {
            if (!developmentValue) {
                throw new Error(`Expected environment variable '${key}' to be set with a development value but none was found.`);
            }
            return developmentValue;
        }
        const value = process.env[key];
        if (!value || value === "") {
            throw new Error(`Expected environment variable '${key}' in production but none was found.`);
        }
        return value;
    };
}
exports.default = proclaim;
